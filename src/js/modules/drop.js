const drop = () => {
	//drag *
	//drahed *
	//dragenter - объект над dropArea
	//drageexit *
	//dragleave - Объект перетащили за предлами dropArea
	//dragover - Объект зависает над dropArea
	//dragstart *
	//drop - Объект отпущен в dropArea

	const fileInputs = document.querySelectorAll('[name="upload"]');
};

export default drop;